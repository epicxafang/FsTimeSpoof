name: Build

on:
  push:
    branches: [main]
    paths:
      - 'FsTimeSpoof.c'
      - 'Makefile'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout FsTimeSpoof
        uses: actions/checkout@v5

      - name: Checkout KernelPatch (headers only)
        uses: actions/checkout@v5
        with:
          repository: bmax121/KernelPatch
          path: KernelPatch
          sparse-checkout: |
            kernel/include/
            kernel/patch/include/
            kernel/linux/include/
            kernel/linux/arch/arm64/include/
            kernel/linux/tools/arch/arm64/include/

      - name: Download minimal toolchain
        run: |
          gh release download v12.2.0 --repo epicxafang/kp-toolchain --pattern '*.tar.gz'
          tar xzf toolchain-aarch64-none-elf-12.2-minimal.tar.gz
          echo "$GITHUB_WORKSPACE/bin" >> $GITHUB_PATH

      - name: Build
        env:
          TARGET_COMPILE: aarch64-none-elf-
          KP_DIR: ${{ github.workspace }}/KernelPatch
        run: make clean && make

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FsTimeSpoof
          path: FsTimeSpoof.kpm
          retention-days: 30
